<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Health Connect (steps, active calories, heart rate, sleep,
         weight — see lib/core/services/health_service.dart and
         lib/features/health/). Health Connect denies access to any
         data type not explicitly declared here, even if the person
         grants permission at runtime, so each type needs its own line. -->
    <uses-permission android:name="android.permission.health.READ_STEPS"/>
    <uses-permission android:name="android.permission.health.READ_ACTIVE_CALORIES_BURNED"/>
    <uses-permission android:name="android.permission.health.READ_HEART_RATE"/>
    <uses-permission android:name="android.permission.health.READ_SLEEP"/>
    <uses-permission android:name="android.permission.health.READ_WEIGHT"/>
    <!-- Health Connect classifies Steps as an "Activity Recognition"
         data type, which needs this separate, standard Android
         permission in addition to the health.READ_STEPS line above. -->
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION"/>
    <application
        android:label="eatsy"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme"
                />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- Handles the io.eatsy.app://reset-password deep link that
                 Supabase's password-reset email opens (see
                 auth_controller.dart's sendPasswordResetEmail redirectTo).
                 Without this, tapping the emailed link does nothing on
                 Android — the app never even opens. -->
            <intent-filter android:autoVerify="false">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="io.eatsy.app" android:host="reset-password"/>
            </intent-filter>
            <!-- Required by the health package: lets Health Connect show
                 its own permission-request screen on top of this app.
                 Without this, requestAuthorization() from
                 health_service.dart fails silently on Android. -->
            <intent-filter>
                <action android:name="androidx.health.ACTION_SHOW_PERMISSIONS_RATIONALE" />
            </intent-filter>
        </activity>
        <!-- Lets Health Connect's own permissions screen link back to
             Eatsy's privacy policy (docs/privacy-policy.html, hosted via
             GitHub Pages) when someone taps "view app's privacy policy"
             from inside Health Connect rather than from Eatsy itself.
             Google's review for sensitive health permissions checks for
             this being present. Routes to MainActivity per the health
             package's own setup docs — the privacy policy is already one
             tap away from there via Dashboard's profile menu. -->
        <activity-alias
            android:name="ViewPermissionUsageActivity"
            android:exported="true"
            android:targetActivity=".MainActivity"
            android:permission="android.permission.START_VIEW_PERMISSION_USAGE">
            <intent-filter>
                <action android:name="android.intent.action.VIEW_PERMISSION_USAGE" />
                <category android:name="android.intent.category.HEALTH_PERMISSIONS" />
            </intent-filter>
        </activity-alias>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Lets the app check whether the Health Connect app is
             installed at all (health_service.dart checks this before
             offering to connect — older Android versions don't ship it
             preinstalled and need it from the Play Store). -->
        <package android:name="com.google.android.apps.healthdata" />
        <intent>
            <action android:name="androidx.health.ACTION_SHOW_PERMISSIONS_RATIONALE" />
        </intent>
    </queries>
</manifest>